[project]
name = "leetcode-review"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flask==3.0.3",
    "requests==2.32.3",
]

[tool.uv]
dev-dependencies = [
    "pytest==8.2.0",
    "pytest-mock==3.14.0",
]
